---
title: 鉴权
description: 配置鉴权相关设置
---

鉴权配置目前提供一个 Cookie 前缀，用于隔离不同应用的登录状态。

### 文件

`src/config/auth/auth-config.tsx`

```ts
export const COOKIE_PREFIX = "vibe-any"
```

根据你的域名与部署环境修改 `COOKIE_PREFIX`，避免与同域名下其它应用的 Cookie 冲突。


